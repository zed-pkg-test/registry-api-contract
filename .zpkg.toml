[package]
org = "zed-pkg-test"
name = "registry-api-contract"
version = "0.1.0"
description = "Independent api acceptance harness"
license = "MIT"

[package.repository]
vcs = "git"
url = "https://github.com/zed-pkg-test/registry-api-contract"

[install]
dir = ".vendor/.zed"

[publish]
exclude = ["artifacts/**", "vendor/**", ".vendor/**", ".env", ".env.*"]
